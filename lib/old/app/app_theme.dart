import 'package:flutter/material.dart';

class AppTheme {
  //configurações de tema da aplicação
  AppTheme._();
  static const Color appColor = Color.fromARGB(255, 0, 0, 0);
  static const Color fontColor = Colors.white;
}
